# 手动部署指南 - wz-main 项目

## 当前状态

✅ **代码已推送**: 所有修复(包括 userProfile 错误修复)已推送到 GitHub
✅ **本地项目已链接**: 链接到正确的 `wz-main` 项目
❌ **自动部署未触发**: wz-main 项目未配置 GitHub 自动部署集成

## 问题原因

1. **权限限制**: Git 作者 `bingzi@example.com` 没有权限通过 Vercel CLI 直接部署到团队项目
2. **GitHub 集成未配置**: `wz-main` 项目没有配置 GitHub 自动部署 webhook
3. **最后部署**: 21天前 (2025年9月24日)

## 解决方案

### 方案 1: Vercel 控制台手动触发 (推荐 - 最快)

1. 访问 Vercel 控制台: https://vercel.com/snails-projects-d6eda891/wz-main
2. 点击右上角 "Redeploy" 或 "Deploy" 按钮
3. 选择 "Production" 环境
4. 点击确认部署

**预计时间**: 2-3 分钟

### 方案 2: 配置 GitHub 自动部署 (长期解决方案)

1. 访问 Vercel 项目设置: https://vercel.com/snails-projects-d6eda891/wz-main/settings
2. 进入 "Git" 标签页
3. 确认 GitHub 仓库连接:
   - 仓库: `Wang-snail/WZ-main`
   - 分支: `main`
4. 启用 "Automatic deployments from Git"
5. 保存设置

**配置完成后**,以后每次推送到 `main` 分支都会自动触发部署。

### 方案 3: 更新 Git 作者信息 (解决权限问题)

如果需要通过 CLI 部署,需要将 Git 作者邮箱添加到 Vercel 团队:

1. 访问团队设置: https://vercel.com/teams/snails-projects-d6eda891/settings/members
2. 邀请 `bingzi@example.com` 或使用正确的 Git 邮箱
3. 接受邀请后,即可使用 `vercel --prod` 命令部署

## 验证部署

部署完成后,检查以下内容:

### 1. 检查部署状态
```bash
vercel ls --prod
```
应该看到一个新的部署(Age 显示为 "1m" 或 "now")

### 2. 验证网站
访问 https://www.wsnail.com 并:
- [ ] 首页正常加载,无错误提示
- [ ] 浏览器控制台无 "userProfile is not defined" 错误
- [ ] 导航到 https://www.wsnail.com/kajian-lessons
- [ ] 卡吉安经验库页面正常显示
- [ ] 可以查看示例数据
- [ ] 点击"添加经验"按钮,表单正常弹出

### 3. 检查关键修复
打开浏览器开发者工具 (F12),检查:
- Console 标签: 应该没有红色错误
- Network 标签: `/data/kajian_lessons.json` 应该返回 200 状态

## 已修复的问题

本次部署包含以下修复:

1. ✅ **HomePage.tsx:211** - 修复 `userProfile is not defined` 错误
   - 从: `userId={userProfile?.userId}`
   - 到: `userId={undefined}`

2. ✅ **ErrorBoundary.tsx** - 增强错误显示
   - 生产环境显示详细错误信息
   - 添加可折叠的错误详情

3. ✅ **websiteConfig.ts** - 安全检查
   - 安全检查 `import.meta.env`
   - 防止在某些环境下出现 undefined 错误

4. ✅ **完整的卡吉安功能**
   - 数据文件: `public/data/kajian_lessons.json`
   - 列表页面: `src/pages/KajianLessonsPage.tsx`
   - 详情页面: `src/pages/KajianLessonDetailPage.tsx`
   - 表单组件: `src/components/KajianLessonForm.tsx`
   - 服务层: `src/services/kajianService.ts`
   - 路由集成: 已添加到 `src/App.tsx`
   - 导航入口: 已添加到 `src/components/Header.tsx`

## 最新提交

```
commit 048c13ce
Author: bingzi <bingzi@example.com>
Date: 2025-10-15 15:02:53

🚀 强制部署:修复 userProfile 错误
```

## 后续建议

1. **配置自动部署**: 按照方案2配置,避免每次手动触发
2. **更新 vercel.json**: 移除已弃用的 `name` 字段
3. **环境变量**: 在 Vercel 控制台配置所需的环境变量
4. **监控**: 设置 Vercel 部署通知,及时了解部署状态

## 联系信息

- 项目 ID: `prj_OXALain1SCUD0EJtGviaB4yHNZMz`
- 团队 ID: `team_W4MxdTuAtOWNfVacOgrWZok5`
- GitHub 仓库: https://github.com/Wang-snail/WZ-main
- 生产域名: https://www.wsnail.com

---

**最后更新**: 2025-10-15 15:03
**状态**: 等待手动部署触发
